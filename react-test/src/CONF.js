const API = process.env.API;
console.log(process.env.NODE_ENV)
console.log(process.env.API)
console.log(process.env.PLAGE)
//const NODE_ENV = process.env.NODE_ENV;
let CONF = {
  HOST: '',
  WITH_CREDENTIALS: false
}

export default CONF;